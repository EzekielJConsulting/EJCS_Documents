const state = {}
const getters = {
  // test: ({ test }) => test,
}
const actions = {}
const mutations = {}

export default {
  state,
  getters,
  actions,
  mutations
}

if (process.env.NODE_ENV !== "production") {
  state
}